#include "stdio.h"
#include "math.h"


void pov (int a, int b)
{
   int result = pov(a,b);
   printf("%d", result);
}

int main()
{

pov(5, 2);


// double result;
// scanf("%d %d", &a,&b);
// result = pow(a,b);
// printf("%lf", result);
   return 0;
}
